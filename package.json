{
  "name": "robs-world",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet tool restore && dotnet fable watch src -s --run vite",
    "build": "dotnet tool restore && dotnet fable src --run vite build"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "use-sync-external-store": "^1.2.2"
  }
}